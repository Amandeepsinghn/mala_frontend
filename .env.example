# Browser requests go through Next.js proxy (same-origin, no CORS issues)
NEXT_PUBLIC_API_URL=/api/v1

# Server-side requests hit backend directly
API_INTERNAL_URL=http://127.0.0.1:8002/api/v1

# Set to "true" only if you want mock data when API fails
# NEXT_PUBLIC_USE_MOCK=true
